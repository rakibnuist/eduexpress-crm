[build]
builder = "nixpacks"
buildCommand = "npm install && npm run build"

[deploy]
startCommand = "npm run start:production"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[[envs]]
name = "production"

[envs.production.variables]
NODE_ENV = "production"
DB_PATH = "/data/crm.db"
