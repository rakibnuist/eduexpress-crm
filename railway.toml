[build]
builder = "nixpacks"
buildCommand = "npm ci && npm rebuild better-sqlite3 && npm run build"

[deploy]
startCommand = "NODE_ENV=production DB_PATH=/data/crm.db node server.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
