[phases.setup]
nixPkgs = ["nodejs_22", "python3", "gcc", "gnumake"]

[phases.build]
cmds = ["npm install --include=dev", "npm rebuild better-sqlite3", "npm run build"]

[start]
cmd = "npm run start:production"
